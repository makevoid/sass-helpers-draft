guard "sass", input: "sass", output: "css", smart_partials: true
guard "haml", input: "haml", output: "html" do
  watch %r{haml/.+\.haml}
end
guard "livereload" do
  watch %r{html/.+\.html}
  watch %r{css/.+\.css}
end